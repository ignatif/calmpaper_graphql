# Migration `20200617001937-ratings`

This migration has been generated by ignatif at 6/17/2020, 12:19:37 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200617001754-rating..20200617001937-ratings
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:./dev.db"
 }
 model User {
@@ -15,9 +15,9 @@
   avatar         String?
   books          Book[]
   chapters       Chapter[]
   voices         Voice[]
-  rating         Rating[]
+  ratings        Rating[]
 }
 model Book {
   id             Int        @default(autoincrement()) @id
@@ -28,9 +28,9 @@
   author         User?      @relation(fields: [authorUsername], references: [username])
   authorUsername String?
   chapters       Chapter[]
   voices         Voice[]
-  rating         Rating[]
+  ratings        Rating[]
 }
 model Chapter {
   id             Int        @default(autoincrement()) @id
@@ -42,9 +42,9 @@
   authorUsername String?
   book           Book?      @relation(fields: [bookId], references: [id])
   bookId         Int?
   voices         Voice[]
-  rating         Rating[]
+  ratings        Rating[]
 }
 model Voice {
   id             Int        @default(autoincrement()) @id
@@ -55,9 +55,9 @@
   book           Book?      @relation(fields: [bookId], references: [id])
   bookId         Int?
   chapter        Chapter?   @relation(fields: [chapterId], references: [id])
   chapterId      Int?
-  rating         Rating[]
+  ratings        Rating[]
 }
 model Rating {
   id             Int        @default(autoincrement()) @id
```


